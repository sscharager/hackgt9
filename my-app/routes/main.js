import React from 'react';
import LoginPage from './loginpage';
import RegisterPage from '/registerpage';

const Main = () => (
    <main>
        <LoginPage />
        <RegisterPage />
    </main>
);

export default Main;
